const player = {
    name: "dongdu",
    point: 412,
    pretty: true,
    fat: false,
};

console.log(player);
console.log(player.name);
console.log(player["pretty"]);

player.lastName = "Lee"; 
console.log(player);